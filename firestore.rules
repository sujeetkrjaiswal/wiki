service cloud.firestore {
  match /databases/{database}/documents {
  	match /users/{user} {
    	allow read;
      allow write: if request.auth.uid == user
    }
    match /perferences/{user} {
    	allow read;
      allow write: if request.auth.uid == user
    }
    match /{document=**} {
      allow read, write: if request.auth.uid != null;
    }
  }
}
